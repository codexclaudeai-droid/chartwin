{
  "name": "my-chart-lib",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server/dev-full.mjs",
    "frontend:dev": "vite",
    "backend:dev": "node server/data-gateway.mjs",
    "prebuild": "node scripts/guard-cloudflare-pages-build.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "preview:cloudflare": "npm run build && node scripts/guard-cloudflare-deploy.mjs && wrangler pages dev dist",
    "deploy:cloudflare": "npm run build && node scripts/guard-cloudflare-deploy.mjs && wrangler pages deploy dist --project-name chart-engine-dev",
    "deploy:cloudflare:prod": "node scripts/guard-cloudflare-deploy.mjs --deny-prod-deploy"
  },
  "devDependencies": {
    "@types/moo": "^0.5.10",
    "@types/nearley": "^2.11.5",
    "typescript": "~6.0.2",
    "vite": "^8.0.4",
    "wrangler": "^4.93.0"
  },
  "dependencies": {
    "moo": "^0.5.3",
    "nearley": "^2.20.1",
    "technicalindicators": "^3.1.0"
  }
}
